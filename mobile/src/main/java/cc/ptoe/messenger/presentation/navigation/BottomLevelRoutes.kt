package cc.ptoe.messenger.presentation.navigation

object BottomLevelRoutes {
    val routes = setOf(
        Screen.Conversations.route,
        Screen.Agents.route,
        Screen.Settings.route
    )
}
